# Story 1.22: Branding Artifact Suite Alignment

**Epic:** Quality and Operations
**Story ID:** 1.22
**Priority:** Medium
**Points:** 5
**Status:** Ready for Review
**Type:** Documentation

## Executor Assignment

executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["manual-review"]

## Story

**As a** product operator and future designer,  
**I want** the supporting branding artifacts to match the new TikTok Scrapper branding book and the real repo surfaces,  
**so that** manifesto, color, type, writing, and design-system guidance stop referencing another product and can be used safely in future implementation work.

## Acceptance Criteria

1. The branding outputs in `squads/branding/output/` no longer reference `Agente Mint`, unrelated Next.js files, or non-existent product surfaces.
2. The updated documents derive their guidance from current repo artifacts such as `docs/prd.md`, `frontend/tailwind.config.js`, `frontend/src/`, and the current dashboard/API surfaces.
3. The package documents the current brand state, known gaps, and realistic next steps without claiming future-state implementation as already shipped.

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `.aiox-core/core-config.yaml`.
> Quality validation will use manual review process only.

## Tasks / Subtasks

- [x] Audit stale branding outputs against the current repo (AC: 1, 2)
  - [x] Review supporting branding files under `squads/branding/output/`
  - [x] Cross-check current Vite, HTML template, Streamlit, and API surfaces
- [x] Replace core brand guidance artifacts (AC: 1, 2)
  - [x] Align manifesto, color system, type system, and UX writing guide
  - [x] Align identity gap analysis and consistency reporting
- [x] Align design-system support artifacts (AC: 2, 3)
  - [x] Replace token architecture, spacing system, motion system, and gate decision
  - [x] Keep recommendations grounded in current repo files and current rollout phases
- [x] Verify and close out the story
  - [x] Run `npm run lint`
  - [x] Run `npm run typecheck`
  - [x] Run `npm test`
  - [x] Update completion notes and file list

## Dev Notes

### Source of Truth

- `squads/branding/output/branding-book.md` is the current parent brand source of truth.
- `docs/prd.md` defines the product and CLI-first positioning.
- `frontend/tailwind.config.js` defines the current implemented Vite palette and baseline visual tokens.
- `frontend/src/` contains the current public and signed-in React surfaces.
- `apps/dashboard/app.py`, `apps/dashboard/pages/`, and `apps/dashboard/templates/` show the current Python read surfaces and template experiments.

### Constraints

- Do not reintroduce old product language or non-existent file references.
- Distinguish clearly between current implementation and target direction.
- Keep the docs usable for both Vite and Python surfaces.

### Testing

- Run `npm run lint`, `npm run typecheck`, and `npm test` before closing the story.
- This story is documentation-only and does not require new runtime tests.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-22 | 0.1 | Initial supporting-branding-artifact alignment created and completed against the current TikTok Scrapper repo surfaces | Codex |

## Dev Agent Record

### Agent Model Used

- GPT-5 Codex

### Debug Log References

- `npm run lint`
- `npm run typecheck`
- `npm test`

### Completion Notes List

- Added a continuation story so the broader branding artifact cleanup remains traceable under the repo's story workflow.
- Replaced stale supporting branding outputs that still referenced another product and unrelated implementation paths.
- Updated the parent branding book header so it now reflects the aligned branding suite instead of warning about stale sibling files.
- Aligned manifesto, color, typography, writing, consistency, gap, spacing, motion, token, and gate documents to the current Creator Product Radar / Estudio do Criador repo surfaces.
- Kept the documents explicit about current reality versus future rollout direction, especially around typography, semantic token extraction, and Python surface parity.
- Re-ran repository quality gates and recorded the current blockers rather than masking them as branding-document issues.

### File List

- `docs/stories/1.22.branding-artifact-suite-alignment.md`
- `squads/branding/output/branding-book.md`
- `squads/branding/output/brand-manifesto.md`
- `squads/branding/output/color-system.md`
- `squads/branding/output/type-system.md`
- `squads/branding/output/ux-writing-guide.md`
- `squads/branding/output/identity-gap-analysis.md`
- `squads/branding/output/consistency-report.md`
- `squads/branding/output/token-architecture.md`
- `squads/branding/output/spacing-system.md`
- `squads/branding/output/motion-system.md`
- `squads/branding/output/gate-decision.md`

## QA Results

- BLOCKED BY PRE-EXISTING REPO FAILURES
- Verification attempted:
  - `npm run lint`
  - `npm run typecheck`
  - `npm test`
- Results:
  - `npm run lint` failed on unused imports in `apps/api/main.py` and an unsorted import block in `ingestion/scrapers/pro100chok_shop.py`
  - `npm run typecheck` failed on a duplicate `_build_apify_record` definition in `bin/radar.py` and a stale `ranking_service` attribute expectation in `tests/integration/test_api_frontend_contracts.py`
  - `npm test` failed in three pre-existing tests:
    - `tests/integration/test_api_frontend_contracts.py::test_offline_ranking_entries_accept_classification_alias`
    - `tests/smoke/test_cli_weekly_run.py::test_cli_weekly_run_outputs_json_summary`
    - `tests/test_pipeline.py::test_config_requires_tiktok_credentials`
- Notes:
  - No failing gate was introduced by the branding documentation files in this story
  - This story is documentation-only and is ready for review once the shared repo blockers are handled independently
