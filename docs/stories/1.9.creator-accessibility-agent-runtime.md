# Story 1.9: Creator Accessibility Agent Contract and Runtime

**Epic:** Scoring Pipeline
**Story ID:** 1.9
**Priority:** High
**Points:** 8
**Status:** Review
**Type:** Feature

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["manual-review"]

## Story

**As a** project operator,
**I want** a working Creator Accessibility Agent runtime on top of the shared scoring contracts,
**so that** the weekly pipeline can produce a third structured agent score before final aggregation is introduced.

## Acceptance Criteria

1. A deterministic Creator Accessibility Agent runtime exists and returns a normalized score plus structured reasoning.
2. The weekly LangGraph flow executes the Creator Accessibility Agent after Viral Potential and exposes the count of scored products in the run summary.
3. Tests cover Creator Accessibility scoring behavior and weekly-run integration with the Creator Accessibility Agent enabled.

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `.aiox-core/core-config.yaml`.
> Quality validation will use manual review process only.

## Tasks / Subtasks

- [x] Implement Creator Accessibility Agent runtime (AC: 1)
  - [x] Build Creator Accessibility inputs from persisted signals and product metadata
  - [x] Return deterministic normalized score and reasoning
- [x] Integrate Creator Accessibility into weekly orchestration (AC: 2)
  - [x] Add Creator Accessibility node after Viral Potential
  - [x] Expose scored-product count in the weekly summary
- [x] Add tests and verification notes (AC: 3)
  - [x] Cover Creator Accessibility runtime behavior
  - [x] Cover weekly-run integration with Creator Accessibility enabled

## Dev Notes

### Implementation Context

- Story 1.8 already introduced the Viral Potential Agent on the shared scoring contract.
- `docs/scoring-model.md` defines Creator Accessibility as difficulty for a smaller creator to sell, authority requirement, and price/friction profile.
- `docs/agents.md` defines Creator Accessibility as a runtime interpretation layer on top of deterministic signals and metadata.

### Constraints

- Keep this story limited to Creator Accessibility only.
- Do not implement final score aggregation, class bands, or persistence of the final explainability payload here.
- Preserve deterministic behavior behind the shared scoring contract.

### Testing

- Use sqlite-backed tests where DB reads are involved.
- Keep CLI output structured JSON.
- Update `docs/project-tracker.md` after implementation.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-13 | 0.1 | Initial Creator Accessibility runtime story created | Codex |
| 2026-03-13 | 0.2 | Creator Accessibility runtime and weekly integration implemented | Codex |

## Dev Agent Record

### Agent Model Used

GPT-5 Codex

### Debug Log References

- None

### Completion Notes List

- Story created before Creator Accessibility implementation to preserve AIOX flow.
- Added a deterministic Creator Accessibility runtime that scores audience fit, price friction, and authority requirement using the shared scoring contract.
- Reused persisted product metadata and deterministic signals to keep the runtime aligned with the Trend and Viral agent inputs.
- Integrated Creator Accessibility into the weekly LangGraph flow after Viral Potential and exposed `creator_products_scored` plus `creator_top_score` in the run summary.
- Added regression coverage for Creator Accessibility scoring, weekly graph execution, weekly run service behavior, and CLI smoke output.
- `python -m compileall services tests bin` passed.
- `python -m uv run ruff check apps bin services tests` passed.
- `python -m uv run mypy` passed.
- `python -m uv run pytest -q` passed with `25 passed`.
- `python -m uv run radar db-upgrade` passed against `sqlite:///C:/Users/Pichau/Desktop/Tiktok Scrapper/.tmp/story_1_9_cli.sqlite3`.
- `python -m uv run radar ingest-mock --profile smoke --count 2` passed.
- `python -m uv run radar weekly-run --profile smoke --week-start 2026-03-09` passed with `creator_products_scored=2` and `creator_top_score=95`.
- Final CLI verification query against `.tmp/story_1_9_cli.sqlite3` returned `products=2`, `product_snapshots=2`, `product_signals=10`, `ingestion_jobs=1`, and `pipeline_runs=1`.
- LangGraph and LangChain Core still emit upstream Python 3.14 warnings during tests, but the project validations pass.

### File List

- `docs/stories/1.9.creator-accessibility-agent-runtime.md`
- `docs/project-tracker.md`
- `services/agents/__init__.py`
- `services/agents/runtime/__init__.py`
- `services/agents/runtime/creator_accessibility_agent.py`
- `services/orchestration/state/weekly_state.py`
- `services/orchestration/graphs/weekly_graph.py`
- `services/orchestration/weekly_run.py`
- `tests/unit/test_creator_accessibility_agent.py`
- `tests/integration/test_weekly_graph.py`
- `tests/integration/test_weekly_run_service.py`
- `tests/smoke/test_cli_weekly_run.py`

## QA Results

- Manual review complete.
- Static validation complete: `ruff` and `mypy` both passed.
- Automated validation complete: `pytest -q` passed with `25 passed`.
- CLI verification complete for `db-upgrade`, `ingest-mock`, and `weekly-run`.
- Residual warning only: upstream `langgraph/langchain_core` emits Python 3.14 deprecation warnings during tests.
